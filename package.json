{
  "name": "nebulaapi",
  "version": "1.0.0",
  "description": "A brief description of your project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests yet!\" && exit 0", // Simple placeholder message for tests
    "deploy": "node deploy.js" // Change according to what is applicable
  },
  "dependencies": {
    "express": "^5.1.0" // Keep only what is really necessary
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0" // Recommended to use the most recent version
  },
  "license": "MIT"
}
